<UserControl x:Class="PL.Customers.Table"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="600">
    <Grid>
        <DataGrid Name="dataGrid"  AutoGenerateColumns="False" BorderBrush="#FF00B9FF" HorizontalGridLinesBrush="#FF00B9FF" VerticalGridLinesBrush="#FF00B9FF">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Path=ID}"/>
                <DataGridTextColumn Header="First Name" Binding="{Binding Path=firstName}"/>
                <DataGridTextColumn Header="Last Name" Binding="{Binding Path=lastName}"/>
                <DataGridTextColumn Header="Gender" Binding="{Binding Path=gender}"/>
                <DataGridTextColumn Header="Date of Birth" Binding="{Binding Path=dateOfBirth}"/>

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
